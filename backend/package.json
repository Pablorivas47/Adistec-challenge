{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend apirest",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file .env --watch index.js",
    "dev": "nodemon --env-file .env --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pablo",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.5"
  },
  "devDependencies": {
    "morgan": "^1.10.0"
  }
}
